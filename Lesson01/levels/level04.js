_levelNr = 4;
delay = 500;

function exec() {
	
	// Repetitive tasks can be put in functions. We can call a function at any time. Cool, right?
	// Because we're already inside a function (to make this all work) we need to create the function outside of this one. 
	
}